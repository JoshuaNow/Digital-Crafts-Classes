var a; //You don't need to declare a variable. 
a = 10; // don't do this.    

var a = 10; // I can declare a variable like this and use it later.active

                // use LET instead

let a = 10; // PROPER way declare a variable.

const a = 100;  // value can not be changed, it also has to be defined.

var myCamelCase= "this value"

let a=1, b=2,c=3
console.log(a,b,c)