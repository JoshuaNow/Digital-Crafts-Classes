let numbers = [5, 3, -10, 3, -21, 78, -54, -1, 8];
let positive = numbers.filter((num) => num > 0); // filter with create a new array of all items that the expression return true
console.log(positive);
